# Root environment example for AI Agent Lab.
# Copy values into a local .env file when a step requires them.
# Do not commit real secrets.

APP_NAME=AI Agent Lab
APP_ENV=development

# Backend and frontend service URLs will be finalized during Plan 1.
BACKEND_URL=http://localhost:8000
FRONTEND_URL=http://localhost:5173
